# Gold SQL

Guarde aqui os SQLs versionados da camada `gold`.

Diretriz:
- os SQLs devem ser executáveis no DuckDB local;
- devem criar ou atualizar tabelas no schema `gold`;
- devem servir como fonte reprodutível do fluxo automatizado.
